// Trip configuration constants
export const TRIP_CONFIG = {
  MIN_TRAVELERS: 4,
  DEFAULT_STATUS: 'available',
  DESTINATION: 'Tijuana, Mexico'
};

// Trip status options
export const TRIP_STATUS = {
  AVAILABLE: 'available',
  WAITLIST: 'waitlist',
  CLOSED: 'closed'
};

// Booking status options
export const BOOKING_STATUS = {
  TO_BE_REQUESTED: 'to_be_requested',
  PENDING: 'pending',
  CONFIRMED: 'confirmed'
};

// User status options
export const USER_STATUS = {
  PENDING: 'pending',
  REVIEWED: 'reviewed'
};

// Hospital reference prefix
export const HOSPITAL_REF_PREFIX = 'MBC-';
